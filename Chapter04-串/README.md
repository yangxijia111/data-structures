# 第4章 串

## 📖 章节概述

- **页码范围**: 第 80 - 100 页
- **教材章节**: 《数据结构》（C语言版）第5版·第4章

---

## 📚 知识点总结

### 核心概念

1. 计算机上的非数值处理的对象基本上是字符串数据。在较早的程序设计语言中，字
2. 这样，字符串也就作为一种变量类型出现在越来越多的程序设计语言中，同时也产生了一
3. 然而，现今我们使用的计算机的硬件结构主要是反映数值计算的需要的，因此，在处
4. 理字符串数据时比处理整数和浮点数要复杂得多。而且，在不同类型的应用中，所处理的
5. 字符串具有不同的特点，要有效地实现字符串的处理，就必须根据具体情况使用合适的存
6. 储结构。这一章，我们将讨论一些基本的串处理操作和几种不同的存储结构。
7. 串的逻辑结构和线性表极为相似，区别仅在千串的数据对象约束为字符集。然而，串
8. 的基本操作和线性表有很大差别。在线性表的基本操作中，大多以“单个元素”作为操作
9. 一个元素等；而在串的基本操作中，通常以“串的整体”作为操作对象，例如在串中查找某
10. 对于串的基本操作集可以有不同的定义方法，读者在使用高级程序设计语言中的串
11. 类型时，应以该语言的参考手册为准。在上述抽象数据类型定义的 13 种操作中，串赋值
12. 种操作构成串类型的最小操作子集。即这些操作不可能利用其他串操作来实现，反之，其
13. 基本思想为：在主串 5 中取从第 i(i 的初值为 pos) 个字符起、长度和串 T 相等的子串和串
14. 类似千线性表的顺序存储结构，用一组地址连续的存储单元存储串值的字符序列。
15. 在串的定长顺序存储结构中，按照预定义的大小，为每个定义的串变量分配一个固定长度
16. # define MAX5TRLEN 255 || 用户可在 255 以内定义最大串长
17. 串的实际长度可在这预定义长度的范围内随意，超过预定义长度的串值则被舍去，称之为
18. ＂截断＂。对串长有两种表示方法：一是如上述定义描述的那样，以下标为 0 的数组分鼠
19. 存放串的实际长度，如 PA5CAL 语言中的串类型采用这种表示方法；二是在串值后面加
20. 在这种存储结构表示时如何实现串的操作，下面以串联接和求子串为例讨论之。


---

## 💻 核心算法索引

1. `算法 4. 2 所示。`
2. `算法 4.3 所示。`
3. `算法 4.4 所示。`
4. `算法 4. 1 的基本思想，采用定长顺序存储结构，可以写出不依赖千其他串操作的匹配`
5. `算法 4. 5 的函数过程中，分别利用计数指针 i 和 j 指示主串 5 和模式串 T 中当前`
6. `算法 4. 5 的匹配过程易于理解，且在某些应用场合，如文本编辑等，效率也较高，例`
7. `算法 4. 6 所示，它在形式上和算法 4.5 极为相似。不同之处仅在千：当`
8. `算法 4. 5 的时间复杂度是 0(n * m), 但在一般情况下，其实际的执行时间近`
9. `算法 4. 5 快得多。但是 KMP 算法的最大特点是指示主串的指针`
10. `算法 4.8 所示。此时匹配算法不变。`
11. `算法 4. 10 所示。`
12. `算法 4. 11 、 4. 12 、 4. 13 和 4. 14 所示。`


---

## 📝 章节练习

### 选择题

1. （待补充）请参考教材配套《数据结构题集》获取完整习题。

### 填空题

1. （待补充）请参考教材配套《数据结构题集》获取完整习题。

### 算法设计题

1. （待补充）请参考教材配套《数据结构题集》获取完整习题。

### 应用题

1. （待补充）请参考教材配套《数据结构题集》获取完整习题。

---

## 📖 延伸阅读

- 教材配套《数据结构题集》（严蔚敏）— 本章习题答案
- 教材配套源码 — 各算法C语言实现

---

## ⬅️ 上一章

[3章 栈和队列](../Chapter03-栈和队列/README.md)

## ➡️ 下一章

[第5章 数组和广义表](../Chapter{ch_num+1:02d}-数组和广义表/README.md)

---

*本章节内容从教材中提取整理，建议配合原书系统学习。*

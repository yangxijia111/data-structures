# 第2章 线性表

## 📖 章节概述

- **页码范围**: 第 28 - 54 页
- **教材章节**: 《数据结构》（C语言版）第5版·第2章

---

## 📚 知识点总结

### 核心概念

1. 从第 2 章至第 4 章将讨论线性结构。线性结构的特点是：在数据元素的非空有限集
2. 线性表 (|inear_ |ist) 是最常用且最简单的一种数据结构。简言之，一个线性表是 n 个
3. 线性表中元素的个数 n(n~0) 定义为线性表的长度， n=0 时称为空表。在非空表中
4. 线性表是一个相当灵活的数据结构，它的长度可根据需要增长或缩短，即对线性表的
5. 对上述定义的抽象数据类型线性表，还可进行一些更复杂的操作，例如，将两个或两
6. 线性表的这种机内表示称做线性表的顺序存储结构或顺序映像 (sequentia| map­
7. ping), 通常，称这种存储结构的线性表为顺序表。它的特点是，为表中相邻的元素 a; 和
8. 表的顺序存储结构是一种随机存取的存储结构。
9. 由于高级程序设计语言中的数组类型也有随机存取的特性，因此，通常都用数组来描
10. 述数据结构中的顺序存储结构。在此，由于线性表的长度可变，且所需最大存储空间随问
11. ／／－－－－－线性表的动态分配顺序存储结构－－－－－
12. 在上述定义中，数组指针 e|em 指示线性表的基地址， |ength 指示线性表的当前长度。
13. 顺序表的初始化操作就是为顺序表分配一个预定义大小的数组空间，并将线性表的当前
14. 在这种存储结构中，容易实现线性表的某些操作，如随机存取第 i 个数据元素等。只
15. 是要特别注意的是， C 语言中数组的下标从 "0" 开始，因此，若 L 是 5qList 类型的顺序表，
16. 数据元素 a;-1 和 a; 之间的逻辑关系发生了变化。在线性表的顺序存储结构中，由于逻辑
17. 数据元素 a;-1 、 a; 和 a;+1 之间的逻辑关系发生变化，为了在存储结构上反映这个变化，同
18. 复杂度的基本操作），而移动元素的个数取决千插入或删除元素的位置。
19. 由式 (2-5) 和 (2-6) 可见，在顺序存储结构的线性表中插入或删除一个数据元素，平均
20. 现在我们来讨论 2. 1 节中例 2-1 和例 2-2 的操作在顺序存储结构的线性表中的实现


---

## 💻 核心算法索引

1. `算法 2. 1 的时间复杂度为 0(ListLength(LA) X ListLength(LB) 汃算`
2. `算法 2.2 中含 3`
3. `算法 2. 3) 。 |istsize 指示顺序表当前分配的存储空间大小，一旦因插入`
4. `算法 2.4 所示。`
5. `算法 2.4 和 2.5 可见，当在顺序存储结构的线性表中某个位置上插入或删除一个`
6. `算法 2. 1 的执行时间主要取决于查找函数 LocateE|em 的执行时间。在顺序表 L`
7. `算法 2. 6 所示。从算法 2. 6 中可见。基本操作是“进行两个元素之间的比较"'若`
8. `算法 2. 2 可直接写出形式上极其相似的算法 2. 7 。显然．`
9. `算法 2. 7 中的基本操作为“元素赋值＇＼算法的时间复杂度为 0(La. |ength+ Lb. |ength) 。`
10. `算法 2. 7 中第一个循环语句的循环体作如下修改：以“开关语句“代替”条件语`
11. `算法 2. 7 之所以有线性的`
12. `算法 2.8 的基本操作是比较 j 和 i 并后移指针 p,whi|e 循环体中的语句频度与被查`
13. `算法 2. 8 的时间复`
14. `算法 2. 10 分别为 List |nsert 和 ListDe|ete 在单链表中的实现。`
15. `算法 2. 9 和算法 2. 10 的时间复杂度均为 0(n) 。这是因为，为在第 i 个结`
16. `算法 2.8 的讨论中，我们巳经得知，它的时间复杂度为 0(n) 。`
17. `算法 2. 9 和 2. 10 中，我们还分别引用了 C 语言中的两个标准函数 ma||oc 和 free 。`
18. `算法 2. 11 是一个从表尾到表头逆向`
19. `算法 2. 12 所示。`
20. `算法 2. 12 的时间复杂度和算法 2. 7 相同．但空间复杂度不同。在 1}1`
21. `算法 2. 13 所示。`
22. `算法 2. 9 、 2. 10 类似，所不同的是，`
23. `算法 2. 14 、 2. 15 和`
24. `算法 2. 17 中，只有一个处于双重循环中的循环体（在集合 A 中查找依次输入的`
25. `算法 2. 17 的时间复杂度为`
26. `算法 2. 17 执行的示意图。假设集合 A= (c,b,e,g. f ,d), B= (a:, b, 11, f).`
27. `算法 2. 19 和 2. 18 所示，两者的时间复杂度均`
28. `算法 2.20 和 2.21 所示。`
29. `算法 2.20 和算法 2. 21 分别为算法 2. 9 和算法 2. 12 的改写形式，它们的时间复杂度`


---

## 📝 章节练习

### 选择题

1. （待补充）请参考教材配套《数据结构题集》获取完整习题。

### 填空题

1. （待补充）请参考教材配套《数据结构题集》获取完整习题。

### 算法设计题

1. （待补充）请参考教材配套《数据结构题集》获取完整习题。

### 应用题

1. （待补充）请参考教材配套《数据结构题集》获取完整习题。

---

## 📖 延伸阅读

- 教材配套《数据结构题集》（严蔚敏）— 本章习题答案
- 教材配套源码 — 各算法C语言实现

---

## ⬅️ 上一章

[1章 绪论](../Chapter01-绪论/README.md)

## ➡️ 下一章

[第3章 栈和队列](../Chapter{ch_num+1:02d}-栈和队列/README.md)

---

*本章节内容从教材中提取整理，建议配合原书系统学习。*
